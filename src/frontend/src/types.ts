export interface NavItem {
  id: string;
  path: string;
  title: string;
  icon: string;
  description: string;
}

export interface SectionCard {
  id: string;
  path: string;
  title: string;
  icon: string;
  description: string;
}
